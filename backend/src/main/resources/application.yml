spring:
  application:
    name: tutorias
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/tutorias_bd}
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:2462183}
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    open-in-view: false
      
  mail:
    host: smtp.gmail.com
    port: 587
    username: tutoriasintersedes@gmail.com
    password: jxdr rvij gknt lxkf
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

jwt:
  secret-key: ${JWT_SECRET:change-this-secret-in-local-env}

server:
  port: ${SERVER_PORT:8080}
